<!--index.wxml-->
<view class="container">
  <swiper class ="swiper" interval = "1000" autoplay = "true" circular = "true">
    <block wx:for = "{{banners}}" wx:key = "k"> 
      <swiper-item>
        <image src="{{item}}" class="slide-image" mode="aspectFit"/>
      </swiper-item>
    </block>
  </swiper>
</view>
